name: Demo-script

on: 
  push:
    branches-ignore:
    - '!master'

jobs:

  super-lint:
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
       os: [ubuntu-latest]

    steps:
      - name: Run tests
        run: mvn clean test
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
